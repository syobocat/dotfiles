-- Conky, a system monitor https://github.com/brndnmtthws/conky
--
-- This configuration file is Lua code. You can write code in here, and it will
-- execute when Conky loads. You can use it to generate your own advanced
-- configurations.
--
-- Try this (remove the `--`):
--
--   print("Loading Conky config")
--
-- For more on Lua, see:
-- https://www.lua.org/pil/contents.html

conky.config = {
    alignment = 'top_right',
    background = true,
    cpu_avg_samples = 2,
    default_color = 'white',
    default_outline_color = 'grey',
    default_shade_color = 'white',
    double_buffer = true,
    draw_borders = false,
    draw_graph_borders = true,
    draw_outline = false,
    draw_shades = false,
    extra_newline = false,
    font = 'Cascadia Code:size=12',
    font1 = 'Cascadia Code:Bold:size=36',
    font2 = 'Noto Sans Mono CJK JP:size=9',
    color1 = 'white',
    gap_x = 60,
    gap_y = 60,
    minimum_height = 5,
    minimum_width = 400,
    net_avg_samples = 2,
    no_buffers = true,
    out_to_console = false,
    out_to_ncurses = false,
    out_to_stderr = false,
    out_to_x = true,
    own_window = true,
    own_window_class = 'Conky',
    own_window_type = 'override',
    own_window_argb_visual = true,
    own_window_argb_value = 128,
    pad_percents = 2,
    show_graph_range = false,
    show_graph_scale = true,
    stippled_borders = 0,
    update_interval = 0.2,
    uppercase = false,
    use_spacer = 'none',
    use_xft = true,
}

conky.text = [[
${alignr}${font1}${time %H:%M %S}${font}
${offset 160}${time %Y/%m/%d}${alignr}${exec LANG=C date '+%A'}

${time %Y}:${alignc}${execbar 5,300 expr 100 \* $(date '+%j') / $(date -j -f '%m/%d' '12/31' '+%j')}${alignr}${exec expr 100 \* $(date '+%j') / $(date -j -f '%m/%d' '12/31' '+%j')}%
 ${time %m} :${alignc}${execbar 5,300 expr 100 \* $(date '+%d') / $(date -v -1d -j -f '%m/%d' "$(expr 1 + $(date '+%m'))/1" '+%d')}${alignr}${exec expr 100 \* $(date '+%d') / $(date -v -1d -j -f '%m/%d' "$(expr 1 + $(date '+%m'))/1" '+%d')}%
 ${time %d} :${alignc}${execbar 5,300 expr 100 \* \( 3600 \* $(date '+%H') + 60 \* $(date '+%M') + $(date '+%S') \) / 86400}${alignr}${exec expr 100 \* \( 3600 \* $(date '+%H') + 60 \* $(date '+%M') + $(date '+%S') \) / 86400}%

System Information${hr}
  OS  :${alignr}${sysname}
Kernel:${alignr}${kernel}
Uptime:${alignr}${uptime}

Hardware Utilization${hr} 
CPU : ${alignr}${cpu}%
${cpugraph cpu0 70,400 ffffff ff0000 1 -t}
RAM : ${alignc}${mem}/${memmax}${alignr}${memperc}%
${memgraph 70,400 ffffff ff0000 1 -t}
]]
