map <enter> open

cmd open ${{
	case $(file -Lb --mime-type $f) in
		text/*)
			$EDITOR $fx;;
		image/*)
			feh -T default --start-at $f;;
		*)
			for f in $fx; do xdg-open $f > /dev/null 2> /dev/null & done;;
	esac
}}